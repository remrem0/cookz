<div class="container">
  <div class="row">
    <h1>Book this menu!</h1>
    <div class="col-xs-7">
      <%= render 'form', booking: @booking %>
      <div class="col-xs-5">
        <div class="menu-recap">
          <%= cl_image_tag(@booking.menu.picture) %>
          <p><%= @booking.menu.price %></p>
        </div>
      </div>
    </div>
  </div>
</div>
